#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Aways 0 (success)
 */

int main(void)
{
	int c;

	c = 0;
	while
		(c < 10) {
			putchar(c + 'a');
			c++;
		}
	putchar('\n');
	return (0);
}
